const LOTTIES = {
  LOTTIES_LOADING: require('@/assets/lotties/loading.json'),
};

const ORTHER = {
  DATA_EMPTY: require('@/assets/images/empty_data.png'),
};

export const Images = {
  ...LOTTIES,
  ...ORTHER,
};
